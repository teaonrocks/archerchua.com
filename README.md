# archerchua.com

A clean portfolio base built with Astro, React, Tailwind CSS, and MDX content collections.

## Commands

```sh
pnpm install
pnpm dev
pnpm build
```

Blog posts live in `src/content/blog` and are rendered through `src/pages/blog/[slug].astro`.
